package negocio.factoria.tests;

import static org.junit.Assert.assertNotNull;

import org.junit.Before;
import org.junit.Test;

import negocio.Almacen.SAAlmacen;
import negocio.Cliente.SACliente;
import negocio.Empleado.SAEmpleado;
import negocio.Producto.SAProducto;
import negocio.Proveedor.SAProveedor;
import negocio.Venta.SAVenta;

public class FactoriaNegocioImpTest {
	/*
	 * private FactoriaNegocioImp factoria;
	 * 
	 * @Before public void setUp() { factoria = new FactoriaNegocioImp(); }
	 * 
	 * @Test public void testGeneraSAAlmacen() { SAAlmacen saAlmacen =
	 * factoria.generaSAAlmacen();
	 * assertNotNull("SAAlmacen debería ser creado correctamente", saAlmacen); }
	 * 
	 * @Test public void testGeneraSACliente() { SACliente saCliente =
	 * factoria.generaSACliente();
	 * assertNotNull("SACliente debería ser creado correctamente", saCliente); }
	 * 
	 * @Test public void testGeneraSAEmpleado() { SAEmpleado saEmpleado =
	 * factoria.generaSAEmpleado();
	 * assertNotNull("SAEmpleado debería ser creado correctamente", saEmpleado); }
	 * 
	 * @Test public void testGeneraSAProducto() { SAProducto saProducto =
	 * factoria.generaSAProducto();
	 * assertNotNull("SAProducto debería ser creado correctamente", saProducto); }
	 * 
	 * @Test public void testGeneraSAProveedor() { SAProveedor saProveedor =
	 * factoria.generaSAProveedor();
	 * assertNotNull("SAProveedor debería ser creado correctamente", saProveedor); }
	 * 
	 * @Test public void testGeneraSAVenta() { SAVenta saVenta =
	 * factoria.generaSAVenta();
	 * assertNotNull("SAVenta debería ser creado correctamente", saVenta); }
	 */
}
